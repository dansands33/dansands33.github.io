# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# local caches / logs
.cache/
*.log
node_modules/

# secrets — never commit (none currently present, but guard anyway)
.env
.env.*
*.secret

# working notes / handover docs — not part of the published site
HANDOVER*.md
